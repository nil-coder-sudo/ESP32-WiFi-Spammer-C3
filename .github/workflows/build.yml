name: Build and Release Firmware

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Install ESP32 Platform
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          # 使用最穩定的 2.0.17 版本
          arduino-cli core install esp32:esp32@2.0.17 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: Install U8g2 Library
        run: arduino-cli lib install "U8g2"

      - name: Compile Firmware
        run: |
          arduino-cli compile --fqbn esp32:esp32:esp32c3 \
            --build-property "compiler.c.elf.flags=-Wl,-zmuldefs" \
            --output-dir ./build ./src

      - name: Merge Binaries (Factory Flash File)
        run: |
          pip install esptool
          cd ./build
          esptool.py --chip esp32c3 merge_bin \
            -o ESP32_C3_Spammer_Full_Flash.bin \
            --flash_mode dio --flash_size 4MB \
            0x0000 *.bootloader.bin \
            0x8000 *.partitions.bin \
            0xe000 boot_app0.bin \
            0x10000 *.bin

      - name: Upload Firmware Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ESP32-C3-Full-Flash
          path: build/ESP32_C3_Spammer_Full_Flash.bin
